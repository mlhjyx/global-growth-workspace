# 复制为 .env（.env 不入库）。真实密钥放本地或密钥库，切勿提交。
# 业务代码不直接读取 Provider 密钥——经 Model Gateway / Adapter 注入（母本 ADR-007、硬边界）。

# 本地依赖（对应 infra/docker-compose.dev.yml）
DATABASE_URL=postgresql://ggw:ggw_dev_local_only@localhost:5432/ggw
REDIS_URL=redis://localhost:6379

# 运行环境
NODE_ENV=development

# 模型/数据/媒体 Provider 密钥占位（真实值放 .env，勿提交）；仅 Gateway/Adapter 层读取
# MODEL_GATEWAY_API_KEY=
# OBJECT_STORAGE_ENDPOINT=
