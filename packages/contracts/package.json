{
  "name": "@ggw/contracts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "跨域契约单一事实源：OpenAPI / AsyncAPI 事件 / JSON Schema / Adapter 接口。受保护路径（ADR-102）。",
  "scripts": {
    "contracts:validate": "node scripts/validate.mjs"
  },
  "devDependencies": {
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1",
    "yaml": "^2.9.0"
  }
}
