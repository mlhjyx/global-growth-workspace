{
  "name": "global-growth-workspace",
  "private": true,
  "version": "0.0.0",
  "description": "出海企业 AI 全球客户开发与增长执行平台 — monorepo。产品真相源见 docs/ 母本 v2.1，工程治理见 CLAUDE.md。",
  "packageManager": "pnpm@11.7.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "contracts:validate": "turbo run contracts:validate",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.9.5",
    "turbo": "^2.10.5",
    "typescript": "^7.0.2"
  }
}
